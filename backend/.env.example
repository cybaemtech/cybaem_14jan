# Database Configuration for cPanel Deployment
# INSTRUCTIONS:
# 1. Copy this file to .env (cp .env.example .env)
# 2. Replace the placeholder values with your actual database credentials
# 3. On cPanel: Place the .env file in the backend directory
# 4. NEVER commit the .env file to version control

# Admin Database (for blogs, jobs, gallery, media, etc.)
DB_HOST=82.25.105.94
DB_NAME=cybaemtech_CYB_db
DB_USER=cybaemtech_CYB_db
DB_PASSWORD=your_password_here
DB_PORT=3306

# Leads Database (for contact form submissions)
LEADS_DB_HOST=82.25.105.94
LEADS_DB_NAME=cybaemtech_contact_form_v2
LEADS_DB_USER=cybaemtech_contact_form_v2
LEADS_DB_PASSWORD=your_password_here
LEADS_DB_PORT=3306

# Optional: Debug key for leads endpoint (remove in production for security)
# LEADS_DEBUG_KEY=your_secret_debug_key_here
